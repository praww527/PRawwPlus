openapi: 3.1.0
info:
  title: Api
  version: 0.4.0
  description: PRaww+ Telecom API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication operations
  - name: users
    description: User management
  - name: calls
    description: Call management
  - name: payments
    description: Payment operations
  - name: numbers
    description: Phone number management
  - name: admin
    description: Admin operations
  - name: contacts
    description: Contact management
  - name: verto
    description: FreeSWITCH Verto WebRTC

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /auth/user:
    get:
      operationId: getAuthUser
      tags: [auth]
      summary: Get current authenticated user
      responses:
        "200":
          description: Authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthUser"

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get current user profile
      responses:
        "200":
          description: User profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /users/settings:
    patch:
      operationId: updateUserSettings
      tags: [users]
      summary: Update user call and FreeSWITCH settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserSettings"
      responses:
        "200":
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  settings:
                    $ref: "#/components/schemas/UserSettings"
                required: [message, settings]
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /verto/config:
    get:
      operationId: getVertoConfig
      tags: [verto]
      summary: Get FreeSWITCH Verto WebRTC configuration for the current user
      responses:
        "200":
          description: Verto configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VertoConfig"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /calls:
    get:
      operationId: listCalls
      tags: [calls]
      summary: List call history
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of calls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallListResponse"
    post:
      operationId: makeCall
      tags: [calls]
      summary: Log and initiate a new call
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MakeCallRequest"
      responses:
        "200":
          description: Call record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallRecord"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /calls/{callId}:
    get:
      operationId: getCall
      tags: [calls]
      summary: Get a specific call record
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallRecord"

  /calls/{callId}/end:
    post:
      operationId: endCall
      tags: [calls]
      summary: Signal call ended and record duration / deduct coins
      parameters:
        - name: callId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EndCallRequest"
      responses:
        "200":
          description: Updated call record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallRecord"

  /numbers:
    get:
      operationId: listMyNumbers
      tags: [numbers]
      summary: List user's owned phone numbers
      responses:
        "200":
          description: User's phone numbers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyNumbersResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /numbers/search:
    get:
      operationId: searchNumbers
      tags: [numbers]
      summary: Search available numbers
      parameters:
        - name: country_code
          in: query
          schema:
            type: string
            default: ZA
        - name: number_type
          in: query
          schema:
            type: string
            enum: [local, mobile]
            default: local
        - name: locality
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Available numbers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NumberSearchResponse"
        "403":
          description: Subscription required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Provider error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /numbers/buy:
    post:
      operationId: buyNumber
      tags: [numbers]
      summary: Purchase a phone number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BuyNumberRequest"
      responses:
        "200":
          description: Number purchased
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BuyNumberResponse"
        "400":
          description: Limit reached or invalid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Subscription required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /numbers/{id}:
    delete:
      operationId: removeNumber
      tags: [numbers]
      summary: Remove/release a phone number
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Number removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                required: [message]
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /numbers/change:
    post:
      operationId: changeNumber
      tags: [numbers]
      summary: Initiate number change (R100 fee via PayFast)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeNumberRequest"
      responses:
        "200":
          description: PayFast payment data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayFastPaymentData"

  /payments/subscribe:
    post:
      operationId: initiateSubscription
      tags: [payments]
      summary: Initiate a subscription payment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubscribeRequest"
      responses:
        "200":
          description: PayFast payment data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayFastPaymentData"

  /payments/webhook:
    post:
      operationId: payfastWebhook
      tags: [payments]
      summary: PayFast ITN webhook
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              additionalProperties:
                type: string
      responses:
        "200":
          description: Webhook processed

  /payments/history:
    get:
      operationId: listPayments
      tags: [payments]
      summary: List payment history
      responses:
        "200":
          description: Payment history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentListResponse"

  /credits/topup:
    post:
      operationId: topUpCredits
      tags: [payments]
      summary: Top up wallet coins via PayFast
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TopUpRequest"
      responses:
        "200":
          description: PayFast payment data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PayFastPaymentData"

  /admin/users:
    get:
      operationId: adminListUsers
      tags: [admin]
      summary: Admin - list all users
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of users
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserListResponse"

  /admin/users/{userId}:
    get:
      operationId: adminGetUser
      tags: [admin]
      summary: Admin - get user details
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUserDetail"

  /admin/users/{userId}/adjust-credit:
    post:
      operationId: adminAdjustCredit
      tags: [admin]
      summary: Admin - manually adjust user coins
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdjustCreditRequest"
      responses:
        "200":
          description: Coins adjusted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"

  /admin/stats:
    get:
      operationId: adminGetStats
      tags: [admin]
      summary: Admin - get platform statistics
      responses:
        "200":
          description: Platform stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStats"

  /admin/calls:
    get:
      operationId: adminListCalls
      tags: [admin]
      summary: Admin - list all calls
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 20
        - name: userId
          in: query
          schema:
            type: string
      responses:
        "200":
          description: List of calls
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallListResponse"

  /contacts:
    get:
      operationId: listContacts
      tags: [contacts]
      summary: List user's contacts
      responses:
        "200":
          description: List of contacts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactListResponse"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      operationId: createContact
      tags: [contacts]
      summary: Create a contact
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContactRequest"
      responses:
        "201":
          description: Contact created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactRecord"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /contacts/bulk:
    post:
      operationId: bulkImportContacts
      tags: [contacts]
      summary: Bulk import contacts from phone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkImportContactsRequest"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkImportResult"

  /contacts/{contactId}:
    delete:
      operationId: deleteContact
      tags: [contacts]
      summary: Delete a contact
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Contact deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    ErrorResponse:
      type: object
      properties:
        error:
          type: string
        message:
          type: string
      required: [error]

    AuthUser:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
        name:
          type: string
        profileImage:
          type: string
        isAdmin:
          type: boolean
      required: [id, username, isAdmin]

    UserProfile:
      type: object
      properties:
        id:
          type: string
        username:
          type: string
        name:
          type: string
        profileImage:
          type: string
        email:
          type: string
        coins:
          type: number
          description: Wallet coin balance (1 coin = R0.90)
        subscriptionStatus:
          type: string
          enum: [active, inactive, pending]
        subscriptionPlan:
          type: string
          enum: [basic, pro]
        subscriptionExpiresAt:
          type: string
          format: date-time
          nullable: true
        lastPaymentDate:
          type: string
          format: date-time
          nullable: true
        nextPaymentDate:
          type: string
          format: date-time
          nullable: true
        totalCallsUsed:
          type: integer
        totalCoinsUsed:
          type: number
        isAdmin:
          type: boolean
        extension:
          type: integer
          nullable: true
          description: User's FreeSWITCH extension number (e.g. 1000)
        createdAt:
          type: string
          format: date-time
      required:
        - id
        - username
        - coins
        - subscriptionStatus
        - subscriptionPlan
        - totalCallsUsed
        - totalCoinsUsed
        - isAdmin
        - createdAt

    VertoConfig:
      type: object
      properties:
        wsUrl:
          type: string
          description: FreeSWITCH Verto WebSocket URL (wss://...)
        domain:
          type: string
          description: FreeSWITCH SIP domain
        extension:
          type: integer
          description: User's extension number
        login:
          type: string
          description: SIP login (extension@domain)
        password:
          type: string
          description: User's FreeSWITCH password
        coins:
          type: number
          description: Current coin balance sent to FreeSWITCH at login time
        configured:
          type: boolean
          description: Whether FreeSWITCH is configured on the server
        settings:
          $ref: "#/components/schemas/UserSettings"
      required: [wsUrl, domain, extension, login, password, coins, configured]

    UserSettings:
      type: object
      properties:
        ringtone:
          type: string
          enum: [default, classic, digital, soft, urgent, none]
          description: Ringtone sound for incoming calls
        ringtoneDuration:
          type: integer
          minimum: 5
          maximum: 120
          description: How long the phone rings before timing out (seconds)
        dnd:
          type: boolean
          description: Do Not Disturb — reject all incoming calls
        freeswitchHost:
          type: string
          description: Custom FreeSWITCH host override
        freeswitchPort:
          type: integer
          minimum: 1
          maximum: 65535
          description: Custom FreeSWITCH SIP port override
      required: [ringtone, ringtoneDuration, dnd]

    OwnedNumber:
      type: object
      properties:
        id:
          type: string
        number:
          type: string
        status:
          type: string
      required: [id, number]

    MyNumbersResponse:
      type: object
      properties:
        myNumbers:
          type: array
          items:
            $ref: "#/components/schemas/OwnedNumber"
        maxNumbers:
          type: integer
        plan:
          type: string
        subscriptionActive:
          type: boolean
      required: [myNumbers, maxNumbers, plan, subscriptionActive]

    AvailableNumber:
      type: object
      properties:
        phone_number:
          type: string
        number_type:
          type: string
        region:
          type: string
          nullable: true
        monthly_cost:
          type: string
          nullable: true
        upfront_cost:
          type: string
          nullable: true
        is_premium:
          type: boolean
      required: [phone_number, number_type, is_premium]

    NumberSearchResponse:
      type: object
      properties:
        numbers:
          type: array
          items:
            $ref: "#/components/schemas/AvailableNumber"
        total:
          type: integer
      required: [numbers, total]

    BuyNumberRequest:
      type: object
      properties:
        phone_number:
          type: string
      required: [phone_number]

    BuyNumberResponse:
      type: object
      properties:
        message:
          type: string
        number:
          $ref: "#/components/schemas/OwnedNumber"
      required: [message, number]

    ChangeNumberRequest:
      type: object
      properties:
        oldNumberId:
          type: string
        newPhoneNumber:
          type: string
      required: [oldNumberId, newPhoneNumber]

    CallRecord:
      type: object
      properties:
        id:
          type: string
        userId:
          type: string
        callerNumber:
          type: string
        recipientNumber:
          type: string
        callType:
          type: string
          enum: [internal, external]
        status:
          type: string
          enum: [initiated, ringing, answered, in-progress, completed, failed, missed, cancelled, busy, no-answer]
        duration:
          type: integer
        cost:
          type: number
          description: Coins used (only for external calls)
        fsCallId:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        failReason:
          type: string
          nullable: true
          description: Human-readable reason for non-completed calls (set by ESL orchestrator)
        startedAt:
          type: string
          format: date-time
          nullable: true
        endedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, userId, recipientNumber, callType, status, duration, cost, createdAt]

    CallListResponse:
      type: object
      properties:
        calls:
          type: array
          items:
            $ref: "#/components/schemas/CallRecord"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
        totalPages:
          type: integer
      required: [calls, total, page, limit, totalPages]

    MakeCallRequest:
      type: object
      properties:
        recipientNumber:
          type: string
        notes:
          type: string
        fsCallId:
          type: string
      required: [recipientNumber]

    EndCallRequest:
      type: object
      properties:
        duration:
          type: integer
          description: Call duration in seconds
        status:
          type: string
          enum: [completed, failed, no-answer, busy]
      required: [duration]

    SubscribeRequest:
      type: object
      properties:
        plan:
          type: string
          enum: [basic, pro]
      required: [plan]

    PayFastPaymentData:
      type: object
      properties:
        paymentUrl:
          type: string
        amount:
          type: string
        itemName:
          type: string
        paymentId:
          type: string
        coinsAdded:
          type: integer
        formFields:
          type: object
          additionalProperties:
            type: string
      required: [paymentUrl, amount, itemName, paymentId, formFields]

    TopUpRequest:
      type: object
      properties:
        amount:
          type: number
      required: [amount]

    PaymentRecord:
      type: object
      properties:
        id:
          type: string
        userId:
          type: string
        amount:
          type: number
        coinsAdded:
          type: number
        status:
          type: string
          enum: [pending, completed, failed, cancelled]
        paymentType:
          type: string
          enum: [subscription, topup, number_change]
        subscriptionPlan:
          type: string
          nullable: true
        payfastPaymentId:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        completedAt:
          type: string
          format: date-time
          nullable: true
      required: [id, userId, amount, coinsAdded, status, paymentType, createdAt]

    PaymentListResponse:
      type: object
      properties:
        payments:
          type: array
          items:
            $ref: "#/components/schemas/PaymentRecord"
        total:
          type: integer
      required: [payments, total]

    AdjustCreditRequest:
      type: object
      properties:
        delta:
          type: number
          description: Amount to add (positive) or remove (negative)
        reason:
          type: string
      required: [delta]

    AdminUserListResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AdminUserSummary"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer
        totalPages:
          type: integer
      required: [users, total, page, limit, totalPages]

    AdminUserSummary:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        username:
          type: string
        coins:
          type: number
        subscriptionStatus:
          type: string
        subscriptionPlan:
          type: string
        totalCallsUsed:
          type: integer
        totalCoinsUsed:
          type: number
        isAdmin:
          type: boolean
        extension:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, email, coins, subscriptionStatus, subscriptionPlan, totalCallsUsed, totalCoinsUsed, isAdmin, createdAt]

    AdminUserDetail:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        username:
          type: string
        coins:
          type: number
        subscriptionStatus:
          type: string
        subscriptionPlan:
          type: string
        subscriptionExpiresAt:
          type: string
          format: date-time
          nullable: true
        totalCallsUsed:
          type: integer
        totalCoinsUsed:
          type: number
        isAdmin:
          type: boolean
        extension:
          type: integer
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, email, coins, subscriptionStatus, subscriptionPlan, totalCallsUsed, totalCoinsUsed, isAdmin, createdAt, updatedAt]

    AdminStats:
      type: object
      properties:
        totalUsers:
          type: integer
        totalCalls:
          type: integer
        totalRevenue:
          type: number
        activeSubscriptions:
          type: integer
      required: [totalUsers, totalCalls, totalRevenue, activeSubscriptions]

    ContactRecord:
      type: object
      properties:
        id:
          type: string
        userId:
          type: string
        name:
          type: string
        number:
          type: string
        email:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, userId, name, number, createdAt]

    ContactListResponse:
      type: object
      properties:
        contacts:
          type: array
          items:
            $ref: "#/components/schemas/ContactRecord"
        total:
          type: integer
      required: [contacts, total]

    CreateContactRequest:
      type: object
      properties:
        name:
          type: string
        number:
          type: string
        email:
          type: string
        fromPhone:
          type: boolean
      required: [name, number]

    BulkImportContactsRequest:
      type: object
      properties:
        contacts:
          type: array
          items:
            $ref: "#/components/schemas/CreateContactRequest"
      required: [contacts]

    BulkImportResult:
      type: object
      properties:
        imported:
          type: integer
        skipped:
          type: integer
        total:
          type: integer
      required: [imported, skipped, total]
