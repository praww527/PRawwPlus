# This file is a stray artifact (created by a sed command gone wrong) and is intentionally disabled.
# The active CI/CD pipeline is defined in deploy.yml
# DO NOT add `on:` or `jobs:` here — this file must never run.
